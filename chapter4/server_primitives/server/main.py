"""サーバー側の最小実行確認用スクリプト。"""

# 処理の流れ:
# 1. server 側の最小実行確認用スクリプト。
# 2. 固定文字列を出力し、Python が正常に起動することだけを確認する。
# 3. 本格的な MCP サーバーを起動する前の疎通確認に使う。

def main():
    """エントリーポイントとして固定メッセージを出力する。"""
    print("Hello from server!")


if __name__ == "__main__":
    main()
